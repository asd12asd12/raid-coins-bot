# Raid Coins Bot

1. Install Python.
2. `pip install -r requirements.txt`
3. Put your BotFather token into `bot.py`.
4. Run `python bot.py`.

This is a minimal starter bot. Extend commands as needed.
